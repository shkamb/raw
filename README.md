# raw
A tiny shell for macOS (ARM64) that doesn't use any standard libraries. It talks directly to the Apple Silicon kernel using assembly.

Note: This is a low-level experimental shell. Use with caution.

### How to run 
```bash
make
./raw
